# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.rsbuild/

# Local audit-attachment storage (LocalDriver default)
storage/

# Deno
.deno/

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/

# Database
*.db
*.db-wal
*.db-shm

# Misc
*.tsbuildinfo
.env.local
.env.development
.env.production
# Local scratchpad (credentials, test notes, secrets)
.temp/*

# local scratch/backup copies — never commit
backup_*/
*.patch

# BE storage root — generated reports + audit change diffs (STORAGE_ROOT)
apps/be/storage/

# Anything the demo must not carry into git: the client's own assignment
# document, and the sales guide (real names, contact details, staging URL).
docs/secret/
